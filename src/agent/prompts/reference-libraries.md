## Reference libraries
External document collections (**not** the user's notes): **{{collections}}**.
- **search_references** — search these for **any** relevant information. A collection often holds project-specific knowledge the vault may not have. Treat it as a primary source alongside the vault, and search it whenever the vault doesn't already fully answer the question.
- **read_reference** — read a passage from a result before relying on it.

**Search references with the same query craft as `search_vault`** — give `keywords` (exact anchors) + a natural-language `query` + an `intent`, plus `hyde`/`any_of` when useful. Don't assume a topic isn't covered just because the vault is thin — the answer is often in the references. Search them broadly; the vault holds the user's own context, the references carry the rest. The user picks which are enabled per chat. Cite results per the **Citations** rules above.
